name: Deploy to VPS
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 136.118.193.174
          username: chandrapradeeprcse2427
          key: ${{ secrets.DEPLOY_KEY }}
          script: bash ~/deploy.sh
